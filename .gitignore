# Segredos — chaves de API ficam SÓ no .env, nunca no repo
# Chave de service account do GCP: cinto de segurança contra `git add -A`.
# As fixtures em tests/ continuam versionadas — o padrão aqui é só de credencial.
*service-account*.json
gcp*.json
credenciais*.json
.env
.env.*
!.env.example

# Python
__pycache__/
*.py[cod]
.venv/
*.egg-info/
dist/
build/

# Ferramentas
.mypy_cache/
.ruff_cache/
.pytest_cache/
.coverage
htmlcov/

# Dagster
.dagster_home/
tmp*/

# Dados locais
dados/
*.dump

# intermediários da conversão do handoff (deriváveis)
painel/rascunho/
painel/node_modules/
painel/dist/

painel/public/rio.pmtiles
